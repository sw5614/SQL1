CREATE TABLE `comment`(
`c_idx` INT(10) NOT NULL AUTO_INCREMENT PRIMARY KEY,
`c_writer` varchar(255) NOT NULL,
`c_content` VARCHAR(255) NOT NULL,
`c_order` INT(10) NOT NULL,
`c_depth` INT(10) NOT NULL, 
`c_date` VARCHAR(255) NOT NULL,
`b_idx` INT(10) NOT NULL, 
`u_idx` INT(10) NOT NULL,
FOREIGN KEY (b_idx)  REFERENCES board(b_idx) ON DELETE NO ACTION, 
FOREIGN KEY (u_idx)  REFERENCES user(u_idx) ON DELETE NO ACTION
)ENGINE=INNODB DEFAULT CHARSET=UTF8;
집에서 테이블 작성 
